import 'package:flutter/material.dart';

class AppShadows {
  static List<BoxShadow> mainShadow = [
    const BoxShadow(blurRadius: 10, color: Colors.black38, offset: Offset(8, 10))

    
  ];
  static List<BoxShadow> profileShadow = [
    const BoxShadow(blurRadius: 5, color: Colors.black38, offset: Offset(3, 5))
  ];

}
